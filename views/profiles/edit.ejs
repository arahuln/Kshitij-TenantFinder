<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Profile</title>
</head>
<body>
    <h1>Edit profile</h1>
    <form action="/profiles/<%= profile._id%>?_method=PUT" method="POST">
        <label for="name">Student Name</label>
        <input type="text" class="form-control" name="name" id="name" placeholder="Student Name" value="<%= profile.name%>" >
        
        <label for="email">Email</label>
        <input type="email" class="form-control" name="email" id="email" placeholder="Email" value="<%= profile.email%>" disabled>

        <label for="bio">Bio</label>
        <input type="text" class="form-control" name="bio" id="bio" placeholder="Bio" value="<%= profile.bio%>">

        <label for="phone">Phone Number</label>
        <input type="number" class="form-control" name="phone" id="phone" placeholder="Phone Number" value="<%= profile.phone%>">

        <label for="age">Age</label>
        <input type="number" class="form-control" name="age" id="age" placeholder="Age(in years)" value="<%= profile.age%>">

        <label for="image">Image Address</label>
        <input type="text" class="form-control" name="image" id="image" placeholder="image address" value="<%= profile.image%>">

        <label for="address">Address</label>
        <input type="text" class="form-control" name="address" placeholder="Address" value="<%= profile.address%>">

        <label for="gender">Select Gender</label>
        <select name="gender" id="gender">

            <% for(let gender of genders) {%> 
                <option value="<%=gender%>" <%= profile.gender===gender?'selected':''  %>    ><%= gender  %> </option>  
                  <% } %> 
            
            <!-- <option value="male">male</option>
            <option value="female">female</option>
            <option value="other">other</option> -->
        </select>
        <button class="btn btn-primary mb-2">Submit</button>

    </form>
    <a href="/profiles/<%=profile._id%>">Cancel</a>
</body>
</html>